# CI.yaml --- Check OpenVox module

name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  checks:
    name: OpenVox ${{ matrix.openvox_version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        openvox_version: [8]

    container:
      image: ghcr.io/voxpupuli/voxbox:${{ matrix.openvox_version }}
      volumes:
        - ${{ github.workspace }}:/repo
        - /etc/gitconfig:/etc/gitconfig

    steps:
      - name: Clone repository
        uses: actions/checkout@v6

      - name: Syntax
        run: bundle exec rake -f /opt/voxbox/Rakefile syntax

      - name: Check
        run: bundle exec rake -f /opt/voxbox/Rakefile check

      - name: Lint code
        run: bundle exec rake -f /opt/voxbox/Rakefile lint

      - name: Lint YAML
        if: ${{ hashFiles('data') != '' }}
        run: yamllint data/

      - name: Lint Metadata
        run: bundle exec rake -f /opt/voxbox/Rakefile metadata_lint

      - name: Rubocop
        run: bundle exec rake -f /opt/voxbox/Rakefile rubocop

      - name: Unit Tests
        run: bundle exec rake -f /opt/voxbox/Rakefile spec
